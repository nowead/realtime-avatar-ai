# Node.js / npm / yarn 관련

# 의존성 폴더 (매우 중요! 절대 Git에 올리면 안 됨)
node_modules/

# 로그 파일
logs/
*.log
npm-debug.log*
yarn-debug.log
yarn-error.log
pnpm-debug.log*

# 빌드 결과물 폴더 (소스 코드로부터 생성되므로 Git에 올릴 필요 없음)
dist/
build/
out/
.parcel-cache/ # Parcel 사용 시
.next/ # Next.js 사용 시

# 환경 변수 파일 (API 키 등 민감 정보 포함 가능성이 높음)
.env
.env.*
# 단, 환경 변수 예시 파일은 포함시키는 것이 좋음
!.env.example
!.env.sample

# IDE 및 편집기 설정 파일 (개인 설정이므로 보통 제외)
.idea/
.vscode/
*.sublime-project
*.sublime-workspace
*.suo
*.user
*.userprefs
*.DS_Store # macOS

# 운영체제 관련 파일
Thumbs.db # Windows
ehthumbs.db # Windows
._* # macOS
.Spotlight-V100 # macOS
.Trashes # macOS

# 테스트 관련
coverage/ # 코드 커버리지 리포트

# 캐시 파일
.npm/
.eslintcache
.yarn/cache
.yarn/plugnplay
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz

# 기타 임시 파일이나 로컬 설정 파일 (필요시 추가)
# .tmp/
# *.swp
__pycache__/
*.pyc